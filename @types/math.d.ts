// common types that dont change regardless of context
// i.e a vector is a vector is vector but a Face might hold
// different information depending on the mesh type
type Vector = [number, number, number];
